## 2026-04-27 04:00 UTC Flush

**Trading Bot Updates:**
- Switched VPS python bot data source from TwelveData to OANDA to bypass rate limits (max 8/min) and sync daily High/Low calculations with TradingView timezones.
- Updated Currency Strength Pine Script and Python script to correctly use the previous day's High/Low (`high[1]`, `low[1]`).
- Implemented an "Open Positions Manager" in the bot (evaluates Hold, Watch, Cut based on live gap and power scores).
- Callunk is currently holding BUY positions on AUD/USD and NZD/USD.
- Implemented `/OpenPosisi [PAIR] [ACTION]` command to add/remove watched positions. Pushed all code to Git.

**LPSE & SIRUP Bulukumba:**
- Converted and compiled LPSE Standardization documents from `.md` to `.docx`/`.xlsx` and provided a zip file.
- Confirmed that INAPROC/LKPP sites (`spse.inaproc.id`, `sikap.inaproc.id`) are blocked by Cloudflare WAF. Callunk will use his Admin/Verificator account to manually export vendor data instead of web scraping.
- Updated SIRUP Bulukumba webapp UI: Adjusted Card 3 (added total packages and % of Pagu), reduced table font sizes (`text-xs`), and heavily restructured the vendor detail popup to match his specific screenshot (Badges on top, RUP/Paket codes, then green "DATA REALISASI / PENYEDIA" text with prices).