//@version=5
indicator("CuyPro Currency Power-Matrix", overlay=true)

// ==========================================
// SETTINGS
// ==========================================
lookback = input.int(14, "Lookback Period", minval=1)
show_table = input.bool(true, "Tampilkan Dashboard")
text_size = input.string("Small", "Ukuran Teks", options=["Tiny", "Small", "Normal", "Large"])

// Konversi ukuran teks
size_map = text_size == "Tiny" ? size.tiny : text_size == "Small" ? size.small : text_size == "Normal" ? size.normal : size.large

// ==========================================
// LOGIKA KEKUATAN MATA UANG
// ==========================================
// Kita menghitung Rate of Change (ROC) untuk setiap pair utama

// USD Strength
usd_s = - (ta.roc(request.security("EURUSD", timeframe.period, close), lookback) + 
           ta.roc(request.security("GBPUSD", timeframe.period, close), lookback) + 
           ta.roc(request.security("AUDUSD", timeframe.period, close), lookback) + 
           ta.roc(request.security("NZDUSD", timeframe.period, close), lookback) + 
           ta.roc(request.security("USDCAD", timeframe.period, close), lookback) + 
           ta.roc(request.security("USDCHF", timeframe.period, close), lookback) + 
           ta.roc(request.security("USDJPY", timeframe.period, close), lookback)) / 7

// EUR Strength
eur_s = (ta.roc(request.security("EURUSD", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURGBP", timeframe.period, close), lookback) * -1 + 
         ta.roc(request.security("EURJPY", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURCAD", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURCHF", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURAUD", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURNZD", timeframe.period, close), lookback)) / 7

// GBP Strength
gbp_s = (ta.roc(request.security("GBPUSD", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURGBP", timeframe.period, close), lookback) * -1 + 
         ta.roc(request.security("GBPJPY", timeframe.period, close), lookback) + 
         ta.roc(request.security("GBPCAD", timeframe.period, close), lookback) + 
         ta.roc(request.security("GBPCHF", timeframe.period, close), lookback) + 
         ta.roc(request.security("GBPAUD", timeframe.period, close), lookback) + 
         ta.roc(request.security("GBPNZD", timeframe.period, close), lookback)) / 7

// JPY Strength
jpy_s = - (ta.roc(request.security("USDJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("EURJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("GBPJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("AUDJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("NZDJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("CADJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("CHFJPY", timeframe.period, close), lookback)) / 7

// AUD Strength
aud_s = (ta.roc(request.security("AUDUSD", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURAUD", timeframe.period, close), lookback) * -1 + 
         ta.roc(request.security("GBPAUD", timeframe.period, close), lookback) * -1 + 
         ta.roc(request.security("AUDJPY", timeframe.period, close), lookback) + 
         ta.roc(request.security("AUDCAD", timeframe.period, close), lookback) + 
         ta.roc(request.security("AUDCHF", timeframe.period, close), lookback) + 
         ta.roc(request.security("AUDNZD", timeframe.period, close), lookback)) / 7

// NZD Strength
nzd_s = (ta.roc(request.security("NZDUSD", timeframe.period, close), lookback) + 
         ta.roc(request.security("EURNZD", timeframe.period, close), lookback) * -1 + 
         ta.roc(request.security("GBPNZD", timeframe.period, close), lookback) * -1 + 
         ta.roc(request.security("NZDJPY", timeframe.period, close), lookback) + 
         ta.roc(request.security("NZDCAD", timeframe.period, close), lookback) + 
         ta.roc(request.security("NZDCHF", timeframe.period, close), lookback) + 
         ta.roc(request.security("AUDNZD", timeframe.period, close), lookback) * -1) / 7

// CAD Strength
cad_s = - (ta.roc(request.security("USDCAD", timeframe.period, close), lookback) + 
           ta.roc(request.security("EURCAD", timeframe.period, close), lookback) * -1 + 
           ta.roc(request.security("GBPCAD", timeframe.period, close), lookback) * -1 + 
           ta.roc(request.security("CADJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("AUDCAD", timeframe.//period, close), lookback) * -1 + 
           ta.roc(request.security("NZDCAD", timeframe.period, close), lookback) * -1 + 
           ta.roc(request.security("CADCHF", timeframe.period, close), lookback)) / 7

// CHF Strength
chf_s = - (ta.roc(request.security("USDCHF", timeframe.period, close), lookback) + 
           ta.roc(request.security("EURCHF", timeframe.period, close), lookback) + 
           ta.roc(request.security("GBPCHF", timeframe.period, close), lookback) + 
           ta.roc(request.security("CHFJPY", timeframe.period, close), lookback) + 
           ta.roc(request.security("AUDCHF", timeframe.period, close), lookback) * -1 + 
           ta.roc(request.security("NZDCHF", timeframe.period, close), lookback) * -1 + 
           ta.roc(request.security("CADCHF", timeframe.period, close), lookback) * -1) / 7

// ==========================================
// DASHBOARD RENDERING
// ==========================================
if show_dashboard and barstate.islast
    var table strengthTable = table.new(position.top_right, 2, 9, bgcolor=color.new(color.black, 30), border_width=1, border_color=color.gray)
    
    // Header
    table.cell(strengthTable, 0, 0, "Currency", text_color=color.white, bgcolor=color.rgb(60, 60, 60), text_size=size_map)
    table.cell(strengthTable, 1, 0, "Power", text_color=color.white, bgcolor=color.rgb(60, 60, 60), text_size=size_map)
    
    // Data rows
    // USD
    table.cell(strengthTable, 0, 1, "USD", text_color=color.white, bgcolor=usd_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 1, str.tostring(usd_s, "#.##"), text_color=color.white, bgcolor=usd_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    // EUR
    table.cell(strengthTable, 0, 2, "EUR", text_color=color.white, bgcolor=eur_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 2, str.tostring(eur_s, "#.##"), text_//color=color.white, bgcolor=eur_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    // GBP
    table.cell(strengthTable, 0, 3, "GBP", text_color=color.white, bgcolor=gbp_s > 0 ? color.new(//color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 3, str.tostring(gbp_s, "#.##"), text_color=color.white, bgcolor=gbp_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    // JPY
    table.cell(strengthTable, 0, 4, "JPY", text_color=color.white, bgcolor=jpy_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 4, str.tostring(jpy_s, "#.##"), text_color=color.white, bgcolor=jpy_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    // AUD
    table.cell(strengthTable, 0, 5, "AUD", text_color=color.white, bgcolor=aud_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 5, str.tostring(aud_s, "#.##"), text_color=color.white, bgcolor=aud_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    // NZD
    table.cell(strengthTable, 0, 6, "NZD", text_color=color.white, bgcolor=nzd_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 6, str.tostring(nzd_s, "#.##"), text_color=color.white, bgcolor=nzd_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    // CAD
    table.cell(strengthTable, 0, 7, "CAD", text_color=color.white, bgcolor=cad_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 7, str.tostring(cad_s, "#.##"), text_color=color.white, bgcolor=cad_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    // CHF
    table.cell(strengthTable, 0, 8, "CHF", text_//color=color.white, bgcolor=chf_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
    table.cell(strengthTable, 1, 8, str.tostring(chf_s, "#.##"), text_//color=color.white, bgcolor=chf_s > 0 ? color.new(color.green, 40) : color.new(color.red, 40), text_size=size_//map)
